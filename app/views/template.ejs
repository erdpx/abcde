<!DOCTYPE html>

<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link href="/stylesheets/style.css" rel="stylesheet" type="text/css">
        <link href="/stylesheets/basic.css" rel="stylesheet" type="text/css">
        <script src="/scripts/script.js"></script>

        <title>abcde</title>
    </head>
    <body>
        <header class="header"><h1><a href="/">abcde</a></h1></header>
        <div class="container">
            <input type="checkbox" id="dropdown-menu"/>
            <label for="dropdown-menu"></label>

            <div class="menu" id="menu">
                <nav>
                <% if (!userLoggedIn) { %>
                    <ul class="nav-bar">
                        <li><button id="register" data-target="#register-form">register</button></li>
                        <li><input id="search-box" placeholder="search curriculum" /></li>
                        <li><button id="login" data-target="#login-form">login</button></li>
                    </ul>
                </nav>
                <div id="register-form" class="form">
                    <form action="/register" method="POST">
                        <input name="username" type="text" autofocus placeholder="username" required />
                        <input name="email" type="text" placeholder="example@email.com" required />
                        <input name="password" type="password" placeholder="password" required />
                        <button id="submit-register-form-btn">register</button>
                    </form>
                </div>
                <div id="login-form" class="form">
                    <form action="/login" method="POST">
                        <input name="username" type="text" autofocus placeholder="username" required />
                        <input name="password" type="password" placeholder="password" required />
                        <span class="error"></span>
                        <button type="submit">login</button>
                    </form>
                </div>
                <% } else if (userLoggedIn) { %>
                    <ul class="nav-bar">
                        <li><input id="search-box" placeholder="search curriculum" /></li>
                    </ul>
                </nav>

                <nav>
                    <ul id="user-menu">
                        <li id="user"><a href="/user/<%= userLoggedIn %>"><%= userLoggedIn %></a></li>
                        <li class=""><a href="/curricula/saved">saved curricula</a></li>
                        <li><a href="/curriculum">new curriculum</a></li>
                        <li><a href="/logout">logout</a></li>
                    </ul>
                </nav>
                <% } %>
            </div>

            <% if (flashMsg) { %>
                <div class="msg"><%= flashMsg %></div>
            <% } %>